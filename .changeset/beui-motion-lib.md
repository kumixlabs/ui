---
"@kumix/ui": minor
---

Add beUI (Motion-based animated components) registry under `components/motion/`, shared utilities under `lib/`, and new hooks. Export map now includes `./lib/*` and `./motion/*`.
