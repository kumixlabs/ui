---
"@kumix/ui": patch
---

Add new components: `Attachment`, `Bubble`, `Chart` (Recharts-based), `Combobox`, `DirectionProvider` (RTL/LTR), `InputGroup`, `Marker`, `Message`, `MessageScroller`, and `NativeSelect`.

Adds `@base-ui/react` and `recharts` as peer dependencies for the new components.
