{
  "name": "root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "turbo run clean",
    "clean:all": "turbo run clean:all && echo 'Cleaning root (can take a while)...' && bun -e \"const fs=require('fs');for(const p of ['.turbo','bun.lock','.husky/_','node_modules'])fs.rmSync(p,{recursive:true,force:true})\" && echo Done",
    "prepare": "husky",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "biome check",
    "lint:fix": "biome check --write --unsafe",
    "format": "biome format --write",
    "format:check": "biome format",
    "types:check": "turbo run types:check",
    "version": "changeset version && bun update",
    "release": "bash ./scripts/publish.sh"
  },
  "keywords": [],
  "author": "Kumix Labs <hai@kumix.io>",
  "license": "MIT",
  "devDependencies": {
    "@arethetypeswrong/core": "^0.18.5",
    "@biomejs/biome": "^2.5.10",
    "@changesets/changelog-github": "^1.0.0",
    "@changesets/cli": "^3.0.1",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@kumix/biome-config": "^0.1.0",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.3.0",
    "@types/react-dom": "^19.2.5",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6",
    "publint": "^0.3.24",
    "react-dom": "^19.2.8",
    "rimraf": "^6.1.3",
    "turbo": "^2.10.11",
    "typescript": "catalog:"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ],
    "catalog": {
      "typescript": "6.0.3"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,cjs,mjs,cts,mts}": "biome check --write --no-errors-on-unmatched",
    "*.{md,yml,yaml}": "prettier --write",
    "*.{json,jsonc,html}": "biome format --write --no-errors-on-unmatched"
  },
  "engines": {
    "node": ">=24",
    "bun": ">=1.4.0"
  },
  "packageManager": "bun@1.4.0"
}
