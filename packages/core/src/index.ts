/**
 * Core utilities for the Kumix template.
 *
 * Replace these placeholders with your package's real API.
 */

/** Greet a name. Placeholder export to keep the build meaningful. */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
